{
  "name": "anomhome-overmind",
  "version": "1.0.0",
  "description": "AnomHome Overmind: A self-hosted home dashboard for Linux",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "test": "node --test backend/tests/*.test.js",
    "test-llama": "node test-llama-server.js",
    "validate": "node validate-config.js",
    "setup-local": "echo 'Setting up local AI...' && cp .env.example .env && echo 'Edit .env file and run: npm run validate'",
    "scrape": "node scripts/scrape.js",
    "build-pages": "mkdir -p docs/data && cp -f data/plates.json docs/data/plates.json && cp -f data/plates.txt docs/data/plates.txt"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "dashboard",
    "self-hosted",
    "home-automation",
    "local-ai",
    "llama-server",
    "file-browser",
    "mind-map"
  ],
  "author": "AnomFIN",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "multer": "^2.0.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "playwright": "^1.41.0"
  }
}
